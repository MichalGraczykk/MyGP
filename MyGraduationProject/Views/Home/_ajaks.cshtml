@model IEnumerable<DatabaseAccess.PropValue>
@{ 
    var lista = new List<string>();
    foreach (var item in Model)
    {
        lista.Add(item.VALUE);
    }
}

    @Html.DropDownList("VAL_ID", new SelectList(Model, "VALUE_ID", "VALUE"), htmlAttributes: new { @class = "form-control" })
<hr />